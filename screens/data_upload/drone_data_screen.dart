import 'package:flutter/material.dart';

class DroneDataScreen extends StatelessWidget {
  const DroneDataScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Drone Data Upload')),
      body: const Center(
        child: Text('Drone Data Upload Screen - Coming Soon'),
      ),
    );
  }
}